<Window xmlns="https://github.com/avaloniaui"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d" d:DesignWidth="400" d:DesignHeight="500"
        x:Class="RedSismica.Views.LoginWindow"
        Width="420"
        SizeToContent="Height"
        Icon="/Assets/avalonia-logo.ico"
        Title="Red Sísmica - Iniciar Sesión"
        WindowStartupLocation="CenterScreen"
        CanResize="False"
        Background="#F9FAFB">
    
    <StackPanel VerticalAlignment="Center" Margin="0">
        <!-- Header Background -->
        <Border Background="#2563EB" 
                Height="120"
                CornerRadius="0,0,0,0">
            <StackPanel VerticalAlignment="Center" 
                       HorizontalAlignment="Center">
                <TextBlock Text="🌍 Red Sísmica" 
                           FontSize="32" 
                           FontWeight="Bold" 
                           Foreground="White"
                           HorizontalAlignment="Center"
                           Margin="0,0,0,5" />
                <TextBlock Text="Sistema de Monitoreo" 
                           FontSize="13" 
                           Foreground="#DBEAFE"
                           HorizontalAlignment="Center" />
            </StackPanel>
        </Border>
        
        <!-- Form Container -->
        <StackPanel Margin="32,40,32,32" Spacing="20">
            
            <!-- Username Section -->
            <StackPanel Spacing="8">
                <TextBlock Text="Usuario" 
                           Classes="Label"
                           FontSize="13" 
                           FontWeight="Medium" 
                           Foreground="#6B7280"/>
                <TextBox Name="UsernameTextBox" 
                         Watermark="Ingrese su usuario"
                         Padding="12,10"
                         FontSize="14"
                         BorderThickness="1"
                         BorderBrush="#E5E7EB"
                         CornerRadius="6"
                         Background="White" />
            </StackPanel>
            
            <!-- Password Section -->
            <StackPanel Spacing="8">
                <TextBlock Text="Contraseña" 
                           Classes="Label"
                           FontSize="13" 
                           FontWeight="Medium" 
                           Foreground="#6B7280"/>
                <TextBox PasswordChar="*" 
                         Name="PasswordBox" 
                         Watermark="Ingrese su contraseña"
                         Padding="12,10"
                         FontSize="14"
                         BorderThickness="1"
                         BorderBrush="#E5E7EB"
                         CornerRadius="6"
                         Background="White" />
            </StackPanel>
            
            <!-- Login Button -->
            <Button Name="LoginButton" 
                    Content="Iniciar Sesión" 
                    Click="LoginButton_Click"
                    IsDefault="True"
                    Classes="Primary"
                    Padding="12,12"
                    FontSize="15"
                    FontWeight="SemiBold"
                    Height="42"
                    CornerRadius="6"
                    Background="#2563EB"
                    Foreground="White"
                    Margin="0,8,0,0" />
            
            <!-- Helper Text -->
            <TextBlock Text="¿Olvidó su contraseña?" 
                       FontSize="12" 
                       Foreground="#2563EB"
                       HorizontalAlignment="Center"
                       Cursor="Hand"
                       Margin="0,4,0,0" />
        </StackPanel>
    </StackPanel>
</Window>